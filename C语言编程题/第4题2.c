// 主函数，计算1!+2!+...+10!的和
#include <stdio.h>
main()
{
	int s=0,p=1,i,j;
	// 外层循环：i从1到10，依次计算每一项阶乘
	for(i=1;i<=10;i++){
		// 每次计算新阶乘前重置p为1
		p=1;
		// 内层循环：计算i的阶乘
		for(j=1;j<=i;j++)
		    p*=j;
		s+=p;	// 累加当前阶乘到总和s
	}
	printf("s=%d\n",s);
} 
