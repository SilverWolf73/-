#include <stdio.h>
main()
{

	int i,j,k,n=0;
	// 枚举5元、1元、0.5元的张数
	for(i=1;i<20;i++)			 		// 5元最多19张
			for(j=1;j<96;j++)			// 1元最多95张
				for(k=1;k<98;k++){ 		// 0.5元最多97张
		// 判断是否满足条件
		// 100元的总数为100，5元的数量乘以5
					if(i+j+k==100 && 5*i+j+0.5*k==100){
						printf("5元:%d,1元:%d,0.5元:%d\n",i,j,k);
						n++;// 统计方案数
					}			    
				}
	printf("共有%d种方案",n);// 输出方案总数	
}
