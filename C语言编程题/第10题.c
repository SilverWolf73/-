// 程序打印一个等腰三角形，每行先输出空格再输出星号，实现居中对齐。
#include <stdio.h>
main()
{
	int i,j,k;
	// 外层循环控制行数，共5行
	for(i=1;i<=5;i++){
		// 打印每行前面的空格，实现居中
		for(k=1;k<=10-i;k++) printf("  ");
		// 打印每行的星号，数量为2*i-1
		for(j=1;j<=2*i-1;j++) printf("%2c",'*');
		// 换行
		printf("\n");
	}
}
